import { ReactElement } from "react";
interface Props {
    amount?: number;
    setPaymentCompleted?: any;
}
export default function DonateButton({ amount, setPaymentCompleted, }: Props): ReactElement;
export {};
